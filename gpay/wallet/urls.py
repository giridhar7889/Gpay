from django.urls import path
# from .views import create_wallet,transaction


app_name = 'wallet'

urlpatterns = [
    # path('create/',create_wallet.as_view(),name="create_wallet"),
   #  path('deposit/',transaction.as_view(),name="deposit"),
   #  path('withdraw/',transaction.as_view(),name="withdraw")
   # # path('deposit/',transcation.as_view(),name="transcation")




]